.side-bar
  .side-bar__user-name
    .side-bar__user-name__top-items
      .name
        = current_user.name
      .icons
        = link_to "a" do
          = icon('far', 'edit', class: 'icon')
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog', class: 'icon')
  .side-bar__group-list
    .side-bar__group-list__group-items
      .group-name
        group name   
      .new-message
        まだメッセージがありません